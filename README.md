# Anusha Chandra — Portfolio (React)

## Run locally
1. Install Node.js LTS from https://nodejs.org (if not installed)
2. `npm install`
3. `npm run dev` → open the printed localhost URL

## Deploy (Vercel)
Push this folder to a GitHub repo, import it on vercel.com — Vite is auto-detected.

## TODOs in code (search "TODO")
- LinkedIn URL (App.jsx, two places)
- Real project screenshots for the CircularGallery (App.jsx galleryItems)
- Optional: custom card faces for the Lanyard via frontImage/backImage props
